[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vahomemath-gis"
version = "0.1.0"
description = "VAHomeMath Growth Intelligence System data foundation"
requires-python = ">=3.9"
dependencies = [
  "alembic>=1.13,<2",
  "psycopg[binary]>=3.1,<4",
  "sqlalchemy>=2.0,<3",
]

[project.optional-dependencies]
dev = [
  "build>=1.2,<2",
  "mypy>=1.10,<2",
  "pytest>=8,<9",
  "ruff>=0.6,<1",
]

[project.scripts]
gis-seed = "gis.seed:main"

[tool.hatch.build.targets.wheel]
packages = ["src/gis"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501", "UP045"]

[tool.mypy]
python_version = "3.9"
strict = true
packages = ["gis"]
